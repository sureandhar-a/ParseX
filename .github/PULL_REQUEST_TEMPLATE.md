## What this pull request does
<!-- One or two sentences: behavior change, fix, or docs. -->

## Checklist

- [ ] Tests added or updated for the change (`ctest --preset default` passes locally)
- [ ] `CHANGELOG.md` entry added under `## [Unreleased]` (see the note at the
  bottom of `CHANGELOG.md` for the expected `Added` / `Changed` / `Fixed` /
  `Removed` format; docs-only PRs skip this box)
- [ ] Docs updated if behavior changed (`README.md`, `docs/cli.md`,
  `docs/mcp.md`, or the design docs in the same PR)
- [ ] Coverage floor holds (`python3 scripts/check_coverage_floor.py`;
  currently 35% — never exclude files to game the number)

## Verification
<!-- Commands run and their result, e.g. ctest output tail. -->
